# Autonomous_Bug_Fixer_Agent
A system that automatically detects, analyzes, and fixes bugs in code by running tests (e.g., pytest), identifying failures, and generating corrected code using AI—reducing manual debugging effort and improving development speed
